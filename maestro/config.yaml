# ============================================================
# Konfigurasi Maestro — project automated (PKPL 2026)
#
# appId: aplikasi default untuk contoh flow (MiLab2, Flutter).
# flows: file/folder yang dijalankan oleh `maestro test maestro/config.yaml`.
# ============================================================

appId: com.example.Milab2

flows:
  - "flows/**"

# Hasil run: screenshot + laporan per test case, dipakai sebagai bukti
# pengujian di laporan praktikum.
testOutputDir: output
